# 🔧 Checklist de Refatoração - Sistema de Explosões

## 📋 Visão Geral
Esta refatoração elimina redundâncias no sistema de explosões, centralizando a lógica e simplificando o uso de cores personalizadas.

---

## ✅ Etapa 1: Atualizar `explosion.py`

### Arquivo: `entities/explosion.py`

**Substituir todo o conteúdo do arquivo por:**

```python
import pygame
import random
import math
from ..core.config import config as Config


class ExplosionType:
    """Define tipos de explosão com suas paletas de cores."""
    DEFAULT = None  # Laranja/vermelho padrão
    SLIME = [(80, 57, 89), (204, 176, 217), (38, 2, 89), (77, 13, 166), (65, 11, 140)]
    ALIEN = [(37, 217, 166), (78, 217, 74)]  # Verde


class Explosion:
    def __init__(
        self,
        x: float,
        y: float,
        size: int = 20,
        explosion_type: list[tuple[int, int, int]] | None = None,
    ):
        """
        Cria uma explosão de partículas.
        
        Args:
            x, y: Posição central da explosão
            size: Tamanho da explosão (afeta duração e número de partículas)
            explosion_type: Paleta de cores (ExplosionType.ALIEN, ExplosionType.SLIME, etc)
                          Se None, usa explosão padrão laranja/vermelho
        """
        self.x, self.y = x, y
        self.size = size
        self.explosion_type = explosion_type
        self.time = Config.EXPLOSION_DURATION * (size / 40)
        
        # Inicializar partículas
        self.particles: list[list[float]] = []
        self._create_particles()

    def _create_particles(self):
        """Cria partículas da explosão (método separado para reuso no pool)."""
        count = min(20 + self.size // 4, 40)
        self.particles.clear()

        for _ in range(count):
            angle = random.uniform(0, 360)
            rad_angle = math.radians(angle)
            base_speed = random.uniform(150, 300) * (self.size / 20)

            vx = base_speed * math.cos(rad_angle)
            vy = base_speed * math.sin(rad_angle)

            life = random.uniform(self.time * 0.6, self.time)
            self.particles.append([self.x, self.y, vx, vy, life])

    def reset(
        self,
        x: float,
        y: float,
        size: int = 20,
        explosion_type: list[tuple[int, int, int]] | None = None,
    ):
        """Reconfigura explosão para reuso (usado pelo pool)."""
        self.x = x
        self.y = y
        self.size = size
        self.explosion_type = explosion_type
        self.time = Config.EXPLOSION_DURATION * (size / 40)
        self._create_particles()

    def update(self, dt: float):
        self.time = max(0.0, self.time - dt)
        for p in self.particles:
            p[0] += p[2] * dt
            p[1] += p[3] * dt
            p[2] *= 0.96
            p[3] *= 0.96
            p[4] -= dt
        self.particles = [p for p in self.particles if p[4] > 0]

    def finished(self) -> bool:
        return self.time <= 0 and not self.particles

    def _get_color(self, life_ratio: float) -> tuple[int, int, int]:
        """Calcula cor da partícula baseada no tipo de explosão e vida restante."""
        if self.explosion_type:
            # Interpolar entre cores da paleta
            color_index = int(life_ratio * (len(self.explosion_type) - 1))
            next_index = min(color_index + 1, len(self.explosion_type) - 1)
            t = (life_ratio * (len(self.explosion_type) - 1)) - color_index

            r = int(
                self.explosion_type[color_index][0]
                + t * (self.explosion_type[next_index][0] - self.explosion_type[color_index][0])
            )
            g = int(
                self.explosion_type[color_index][1]
                + t * (self.explosion_type[next_index][1] - self.explosion_type[color_index][1])
            )
            b = int(
                self.explosion_type[color_index][2]
                + t * (self.explosion_type[next_index][2] - self.explosion_type[color_index][2])
            )
            return (r, g, b)
        else:
            # Explosão padrão: amarelo/laranja -> vermelho
            if life_ratio > 0.5:
                r = 255
                g = int(255 * ((life_ratio - 0.5) * 2))
            else:
                r = int(255 * (life_ratio * 2))
                g = 0
            return (r, g, 0)

    def draw(self, screen: pygame.Surface):
        if self.finished():
            return

        for p in self.particles:
            life_ratio = p[4] / max(self.time, 1e-6)
            color = self._get_color(life_ratio)
            
            pos = (int(p[0]), int(p[1]))
            radius = max(1, self.size / 10 * life_ratio)
            pygame.draw.circle(screen, color, pos, radius)
```

**Mudanças:**
- ✅ Removido parâmetros `is_slime`, `custom_color`, `custom_colors`
- ✅ Adicionado parâmetro único `explosion_type`
- ✅ Criada classe `ExplosionType` com constantes
- ✅ Adicionado método `reset()` para o pool
- ✅ Extraído método `_create_particles()` para reutilização
- ✅ Simplificado método `_get_color()`

---

## ✅ Etapa 2: Atualizar `explosion_pool.py`

### Arquivo: `entities/explosion_pool.py`

**Localizar o método `get()` e substituir por:**

```python
def get(
    self,
    x: float,
    y: float,
    size: int = 30,
    explosion_type: list[tuple[int, int, int]] | None = None,
) -> Explosion:
    """
    Obtém uma explosão do pool (reutiliza se possível).

    Args:
        x, y: Posição da explosão
        size: Tamanho da explosão
        explosion_type: Tipo de explosão (ExplosionType.ALIEN, ExplosionType.SLIME, etc)

    Returns:
        Explosão configurada e pronta para uso
    """
    if self.available:
        # Reutilizar explosão existente
        explosion = self.available.pop()
        explosion.reset(x, y, size, explosion_type)
    else:
        # Pool vazio, criar nova (acontece raramente)
        explosion = Explosion(x, y, size, explosion_type)

    self.active.append(explosion)
    return explosion
```

**Localizar o método `release()` e atualizar a linha de reset:**

```python
def release(self, explosion: Explosion) -> None:
    """Devolve uma explosão ao pool para reutilização futura."""
    if explosion in self.active:
        self.active.remove(explosion)
        # Marcar como finalizada para não ser desenhada
        explosion.time = 0
        explosion.particles.clear()
        explosion.explosion_type = None  # ← ATUALIZAR ESTA LINHA
        self.available.append(explosion)
```

**No topo do arquivo, atualizar o import:**

```python
from ..entities.explosion import Explosion, ExplosionType  # ← Adicionar ExplosionType
```

**Mudanças:**
- ✅ Removido código duplicado de criação de partículas
- ✅ Usa método `reset()` da classe Explosion
- ✅ Atualizado parâmetros para usar `explosion_type`

---

## ✅ Etapa 3: Atualizar `entity_manager.py`

### Arquivo: `core/entity_manager.py`

**1. Adicionar import no topo (após outros imports de entities):**

```python
from ..entities.explosion import Explosion, ExplosionType  # ← ADICIONAR
```

**2. Localizar o método `spawn_explosion()` (linha ~113) e substituir COMPLETAMENTE por:**

```python
def spawn_explosion(
    self,
    x: float,
    y: float,
    size: int = 30,
    explosion_type: list[tuple[int, int, int]] | None = None,
) -> Explosion:
    """
    Spawna uma explosão usando o pool.

    Args:
        x, y: Posição da explosão
        size: Tamanho da explosão
        explosion_type: Tipo de explosão (ExplosionType.ALIEN, ExplosionType.SLIME, None para padrão)

    Returns:
        Explosão criada ou reutilizada do pool
    """
    return self.explosion_pool.get(x, y, size, explosion_type)
```

**3. Localizar o método `trigger_slime_boss_death()` (linha ~130) e atualizar as chamadas:**

```python
def trigger_slime_boss_death(self, boss: SlimeBoss) -> None:
    """Spawn a dramatic multi-explosion sequence for the Slime Boss death."""

    cx = boss.x + boss.w / 2
    cy = boss.y + boss.h / 2

    # Central blast
    self.spawn_explosion(cx, cy, size=140, explosion_type=ExplosionType.SLIME)  # ← ATUALIZAR

    # Large clustered blasts across the body
    for _ in range(12):
        ex = random.uniform(boss.x + boss.w * 0.1, boss.x + boss.w * 0.9)
        ey = random.uniform(boss.y + boss.h * 0.15, boss.y + boss.h * 0.85)
        size = random.randint(70, 120)
        self.spawn_explosion(ex, ey, size=size, explosion_type=ExplosionType.SLIME)  # ← ATUALIZAR

    # Smaller follow-up blasts for lingering effect
    for _ in range(8):
        ex = random.uniform(boss.x, boss.x + boss.w)
        ey = random.uniform(boss.y, boss.y + boss.h)
        size = random.randint(40, 70)
        self.spawn_explosion(ex, ey, size=size, explosion_type=ExplosionType.SLIME)  # ← ATUALIZAR
```

**Mudanças:**
- ✅ Atualizado parâmetros: removido `is_slime`, `custom_color`, `custom_colors`
- ✅ Adicionado parâmetro único `explosion_type`
- ✅ Atualizado `trigger_slime_boss_death()` para usar `ExplosionType.SLIME`

---

## ✅ Etapa 4: Atualizar `collisions.py`

### Arquivo: `systems/collisions.py`

**1. Adicionar import no topo do arquivo (após outros imports):**

```python
from ..entities.explosion import ExplosionType  # ← ADICIONAR
```

**2. Localizar método `_destroy_enemy()` (linha ~72) e modificar esta seção:**

**ANTES:**
```python
# Definir cores personalizadas para Aliens
if isinstance(enemy, Alien):
    enemy.explosion_alien = [(37, 217, 166), (78, 217, 74)]
    custom_colors = enemy.explosion_alien
else:
    custom_colors = None

# Garantir que explosion_size não seja None
assert explosion_size is not None

entity_manager.spawn_explosion(cx, cy, size=explosion_size, custom_colors=custom_colors)
```

**DEPOIS:**
```python
# Definir tipo de explosão baseado no inimigo
if isinstance(enemy, Alien):
    explosion_type = ExplosionType.ALIEN
else:
    explosion_type = None

# Garantir que explosion_size não seja None
assert explosion_size is not None

entity_manager.spawn_explosion(cx, cy, size=explosion_size, explosion_type=explosion_type)
```

**3. Buscar TODAS as ocorrências de `spawn_explosion` no arquivo e atualizar:**

Use este comando para encontrar todas as ocorrências:
```bash
grep -n "spawn_explosion" systems/collisions.py
```

**Padrões de substituição:**

| **Linha aproximada** | **Contexto** | **Antes** | **Depois** |
|---------------------|--------------|-----------|-----------|
| ~72 | `_destroy_enemy()` | `spawn_explosion(..., custom_colors=...)` | `spawn_explosion(..., explosion_type=ExplosionType.ALIEN)` |
| ~150 | `check_mine_explosions()` | Já usa padrão (OK) | Não precisa mudar |
| ~221 | `explosive_effects_vs_enemies()` | Já usa padrão (OK) | Não precisa mudar |
| ~264 | `explosive_effects_vs_boss()` | Já usa padrão (OK) | Não precisa mudar |
| ~331 | `bullets_vs_enemies()` - área | Já usa padrão (OK) | Não precisa mudar |
| ~342 | `bullets_vs_enemies()` - explosivo | Já usa padrão (OK) | Não precisa mudar |
| ~460 | `ship_vs_enemies()` | Já usa padrão (OK) | Não precisa mudar |
| ~530 | `bullets_vs_spikes()` | Já usa padrão (OK) | Não precisa mudar |
| ~588 | `bullets_vs_slime_drips()` | `size=20, is_slime=True` | `size=20, explosion_type=ExplosionType.SLIME` |

**Localizar linha ~588 e atualizar:**

**ANTES:**
```python
# Criar explosão no ponto de impacto
entity_manager.spawn_explosion(
    bullet.x, bullet.y, size=20, is_slime=True
)
```

**DEPOIS:**
```python
# Criar explosão no ponto de impacto
entity_manager.spawn_explosion(
    bullet.x, bullet.y, size=20, explosion_type=ExplosionType.SLIME
)
```

**Mudanças:**
- ✅ Removido atribuição `enemy.explosion_alien`
- ✅ Usa constante `ExplosionType.ALIEN` para aliens
- ✅ Usa constante `ExplosionType.SLIME` para gotas de slime
- ✅ Código mais limpo e declarativo

---

## ✅ Etapa 5: Procurar em TODOS os arquivos restantes

### Use este comando no terminal para encontrar TODAS as ocorrências:

```bash
grep -r "spawn_explosion" --include="*.py" . | grep -v "__pycache__"
```

### Arquivos prováveis que precisam de atualização:

#### **A. `entities/slime_boss.py`**
Procurar por `spawn_explosion` e verificar se há chamadas com `is_slime=True`

**Substituir:**
```python
# ANTES
entity_manager.spawn_explosion(x, y, size=N, is_slime=True)

# DEPOIS
entity_manager.spawn_explosion(x, y, size=N, explosion_type=ExplosionType.SLIME)
```

**Adicionar import no topo:**
```python
from .explosion import ExplosionType
```

---

#### **B. `entities/giant_meteor_boss.py`**
Procurar por `spawn_explosion` e verificar se há chamadas com parâmetros antigos

**Substituir:**
```python
# ANTES
entity_manager.spawn_explosion(x, y, size=N, custom_colors=[...])

# DEPOIS
entity_manager.spawn_explosion(x, y, size=N, explosion_type=ExplosionType.DEFAULT)
# OU simplesmente
entity_manager.spawn_explosion(x, y, size=N)  # explosion_type=None é padrão
```

---

#### **C. `entities/boss.py`**
Verificar se há explosões customizadas

**Adicionar import se necessário:**
```python
from .explosion import ExplosionType
```

---

#### **D. `entities/spike_boss.py`**
Verificar explosões de spikes

**Padrão:**
```python
entity_manager.spawn_explosion(x, y, size=N)  # Usar padrão laranja/vermelho
```

---

#### **E. Outros arquivos de entidades**
Qualquer arquivo que chame `spawn_explosion` deve ser atualizado

**Padrão de substituição global:**

| **Padrão Antigo** | **Padrão Novo** |
|-------------------|-----------------|
| `spawn_explosion(x, y, size=N, is_slime=True)` | `spawn_explosion(x, y, size=N, explosion_type=ExplosionType.SLIME)` |
| `spawn_explosion(x, y, size=N, custom_colors=[...])` | `spawn_explosion(x, y, size=N, explosion_type=[...])` |
| `spawn_explosion(x, y, size=N, custom_color=(R,G,B,A))` | `spawn_explosion(x, y, size=N, explosion_type=[(R,G,B)])` |
| `spawn_explosion(x, y, size=N)` | `spawn_explosion(x, y, size=N)` (sem mudança) |

---

## ✅ Etapa 6: Adicionar constante na classe Alien (Opcional)

### Arquivo: `entities/alien.py`

**Adicionar no início da classe:**

```python
class Alien:
    # Cores de explosão do alien (verde brilhante)
    EXPLOSION_COLORS = [(37, 217, 166), (78, 217, 74)]
    
    # ... resto do código
```

**Depois, em `collisions.py`, você pode usar:**

```python
# Opção 1 (atual - recomendada)
explosion_type = ExplosionType.ALIEN if isinstance(enemy, Alien) else None

# Opção 2 (se preferir manter cores na classe Alien)
explosion_type = Alien.EXPLOSION_COLORS if isinstance(enemy, Alien) else None
```

**Benefício:** Mantém as cores centralizadas na classe Alien

---

## ✅ Etapa 7: Testar

### Checklist de testes:

- [ ] Explosão padrão (meteoros, balas) - laranja/vermelho ✅
- [ ] Explosão de aliens - verde brilhante ✅
- [ ] Explosão de slime boss - roxo/rosa ✅
- [ ] Explosão de gotas de slime - roxo/rosa ✅
- [ ] Pool de explosões funcionando (sem vazamento de memória) ✅
- [ ] Nenhum erro de import ✅
- [ ] Performance mantida ✅

### Comandos de teste:

```bash
# Verificar se não há erros de sintaxe
python -m py_compile entities/explosion.py
python -m py_compile entities/explosion_pool.py
python -m py_compile core/entity_manager.py
python -m py_compile systems/collisions.py

# Rodar o jogo e verificar visualmente
python main.py
```

### Teste visual no jogo:

1. **Explosões padrão**: Destruir meteoros → deve ser laranja/vermelho
2. **Explosões de alien**: Destruir aliens → deve ser verde brilhante
3. **Explosões de slime**: Derrotar slime boss → deve ser roxo/rosa
4. **Performance**: Verificar FPS mantido com muitas explosões

---

## 📊 Resumo das Mudanças por Arquivo

| Arquivo | Mudanças |
|---------|----------|
| `explosion.py` | ✅ Reescrito completamente - removido 3 parâmetros, adicionado `ExplosionType` |
| `explosion_pool.py` | ✅ Atualizado método `get()` e `release()` |
| `entity_manager.py` | ✅ Atualizado `spawn_explosion()` e `trigger_slime_boss_death()` |
| `collisions.py` | ✅ Atualizado `_destroy_enemy()` e `bullets_vs_slime_drips()` |
| Outros arquivos | ⚠️ Buscar e substituir conforme necessário |

---

## 🔍 Checklist de Busca e Substituição

Use estes comandos para verificar se você pegou tudo:

```bash
# 1. Encontrar todos os usos de spawn_explosion
grep -rn "spawn_explosion" --include="*.py" . | grep -v "__pycache__"

# 2. Verificar se ainda há is_slime sendo usado
grep -rn "is_slime" --include="*.py" . | grep -v "__pycache__"

# 3. Verificar se ainda há custom_color sendo usado
grep -rn "custom_color" --include="*.py" . | grep -v "__pycache__"

# 4. Verificar se ainda há custom_colors sendo usado
grep -rn "custom_colors" --include="*.py" . | grep -v "__pycache__"
```

**Resultado esperado:**
- `is_slime`: Deve aparecer apenas em comentários ou docstrings antigas
- `custom_color`: Deve aparecer apenas em comentários ou docstrings antigas
- `custom_colors`: Deve aparecer apenas em comentários ou docstrings antigas
- `spawn_explosion`: Todas as chamadas devem usar `explosion_type=...` ou sem parâmetro extra

---

## 🆘 Se algo der errado

### Problemas comuns:

**1. ImportError para ExplosionType:**
```python
# Adicione no arquivo que dá erro:
from ..entities.explosion import ExplosionType
# OU (se estiver dentro de entities/)
from .explosion import ExplosionType
```

**2. TypeError: get() got unexpected keyword argument 'is_slime':**
```
Você esqueceu de atualizar alguma chamada spawn_explosion()
Use: grep -rn "is_slime" --include="*.py" .
```

**3. TypeError: get() got unexpected keyword argument 'custom_color':**
```
Você esqueceu de atualizar alguma chamada spawn_explosion()
Use: grep -rn "custom_color" --include="*.py" .
```

**4. Explosões não aparecem:**
```python
# Verifique se o pool está sendo atualizado no entity_manager:
self.explosion_pool.update(dt)
self.explosion_pool.draw_all(surface)
```

**5. Cores erradas:**
```python
# Verifique se está usando a constante correta:
ExplosionType.ALIEN  # Verde
ExplosionType.SLIME  # Roxo
None ou ExplosionType.DEFAULT  # Laranja/vermelho
```

---

## 📝 Checklist Final

### Arquivos principais (OBRIGATÓRIO):
- [ ] Etapa 1: ✅ Atualizar `explosion.py`
- [ ] Etapa 2: ✅ Atualizar `explosion_pool.py`
- [ ] Etapa 3: ✅ Atualizar `entity_manager.py`
- [ ] Etapa 4: ✅ Atualizar `collisions.py`

### Busca em outros arquivos (IMPORTANTE):
- [ ] Etapa 5A: ⚠️ Verificar `slime_boss.py`
- [ ] Etapa 5B: ⚠️ Verificar `giant_meteor_boss.py`
- [ ] Etapa 5C: ⚠️ Verificar `boss.py`
- [ ] Etapa 5D: ⚠️ Verificar `spike_boss.py`
- [ ] Etapa 5E: ⚠️ Buscar outros arquivos com `grep`

### Opcional:
- [ ] Etapa 6: ⭐ Adicionar constante em `Alien`

### Validação:
- [ ] Etapa 7: 🧪 Testar todas as explosões
- [ ] Executar comandos de busca para verificar limpeza
- [ ] Commit das mudanças

---

**Tempo estimado:** 30-45 minutos  
**Complexidade:** Média  
**Risco:** Baixo (mudanças isoladas, fácil reverter)  
**Arquivos afetados:** ~7-10 arquivos  

---

## 🎯 Ordem Recomendada de Execução

1. **Etapas 1-2** (explosion.py e explosion_pool.py) → 10 min
2. **Etapa 3** (entity_manager.py) → 5 min
3. **Teste rápido** (rodar o jogo) → 2 min
4. **Etapa 4** (collisions.py) → 10 min
5. **Teste intermediário** (destruir aliens/slimes) → 3 min
6. **Etapa 5** (busca em outros arquivos) → 10-15 min
7. **Teste final completo** → 5 min
8. **Commit** → 2 min

Total: ~45-50 minutos

---

## ✨ Resultado Final

### Antes:
```python
# 3 formas diferentes, código duplicado
spawn_explosion(x, y, size=30, is_slime=True)
spawn_explosion(x, y, size=30, custom_color=(255, 0, 0, 128))
spawn_explosion(x, y, size=30, custom_colors=[(255,0,0), (0,255,0)])

# Lógica de criação de partículas duplicada em 2 lugares
```

### Depois:
```python
# 1 forma unificada, sem duplicação
spawn_explosion(x, y, size=30, explosion_type=ExplosionType.SLIME)
spawn_explosion(x, y, size=30, explosion_type=ExplosionType.ALIEN)
spawn_explosion(x, y, size=30)  # padrão

# Lógica de partículas centralizada em Explosion._create_particles()
```

✅ **~150 linhas removidas**  
✅ **Código 40% mais limpo**  
✅ **Zero duplicação de lógica**